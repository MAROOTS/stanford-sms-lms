/**
 * StanfordOS UI Component Library
 * =================================
 * Production-grade, accessible React components for building school management interfaces.
 *
 * Architecture:
 *   Primitives (Button, Input, Select, Badge) → Composed (Card, Modal, Table) → Pages
 *
 * Every component handles: loading, empty, error, disabled, and focus states.
 *
 * Quick start:
 *   import { Button, Card, Input, Table, Modal, Badge, Select, Skeleton } from '@/components/ui';
 */

export { default as Button } from './Button';
export { default as Input } from './Input';
export { default as Select } from './Select';
export { default as Badge } from './Badge';
export { default as Card } from './Card';
export { default as Modal } from './Modal';
export { default as Table } from './Table';
export { default as Skeleton } from './Skeleton';
export { default as tokens } from './theme';
